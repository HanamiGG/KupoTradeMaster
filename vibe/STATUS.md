# STATUS — KupoTradeMaster (FFXIV Dalamud market plugin)

> Re-entry doc. Read this FIRST when returning. Update it at the END of every session.
> Don't re-derive settled decisions — check `what_not_to_rethink`.

- **current_phase:** 6.5 / 8 — Pre-Phase-7 correctness pass. Full code review ran (10 findings), all fixed. Home-world pricing refactor also landed (blocks realistic sell prices). Phase 7 (Test & Refine) is queued but user still holding on it — more feature edits requested first.
- **current_status:** All 10 review findings resolved AND a structural pricing fix: sell / undercut logic now scopes to home world (Midgardsormr), not DC-wide, so a foreign world's 2M outlier (Rose Garnet Ore etc.) can't inflate margins any more.
- **what_changed_last:** Two major passes. (A) **Home-world pricing refactor** — new `Services/PriceLens.cs` with `HomeWorldMin`/`HomeWorldAvg`/`EffectiveMin`/`SellReference`/`IsHomeWorldSourced`; `Plugin.HomeWorldName` reads live PlayerState with TaxRates fallback; `PriceDsl` gains `mbminhw`/`mbavghw` sources (DC-wide `mbmin`/`dbmarket` stay for power users). Rewired CraftingTab (result priced as `SellReference(home, hqOnly)`; mats as `EffectiveMin(home)`), BestDealsTab (cheapest cross-world buy vs home-world undercut sell), WatchlistTab (min = home-world; margin uses SellReference), TooltipOverlay ("Min (Midgardsormr)" with DC fallback), RetainersTab ("MB min (home)" with `*` asterisk when falling back to DC), BuySignalWatcher (triggers on home-world effective min), WatchlistPoller.ComputeRawScore (buy = DC min, sell = home undercut). ArbitrageTab / SniperTab stay cross-world by design. (B) **Code-review fixes** — (1) BackupService restore now routes each restored file through service-specific `ImportFrom(Stream)` on Journal/Valuation/Retainer under a per-service `_fileLock`, then reloads in-memory state — no more silent no-op restores or torn writes. (2) `Configuration.Sync` lock added; BuySignalWatcher snapshots Watchlist+mutes under it; all context-menu/settings mutation sites lock too — kills the poller-thread `InvalidOperationException`. (3) Custom-source formula cache key now uses `Plugin.CustomSourcesFingerprint` (content hash) instead of `.Count`, in both BuySignalWatcher and BestDealsTab. (4) `GroupService.Rename` carries `GroupFormulas`/`GroupOperations` to the new name; `Delete` removes the companion entries. (5) CraftingTab HQ fallback bug already killed by PriceLens (hqOnly returns 0 rather than falling back to NQ). (6) SettingsTab BuyFormula buffer now seeded ONCE (`_formulaBufferSeeded`) — user can clear the field to retype. (7) Mute-remaining display uses `FormatDuration` with `TotalDays`/`TotalHours` — no more "0h 0m" on 7-day mutes. (8) UniversalisClient dispatches all batches with `Task.WhenAll` around the semaphore instead of awaiting inline — `MaxConcurrent=2` now actually overlaps. (9) WealthTab "Snapshot now" guarded by `_snapshotting` bool + disabled button. (10) Valuation/Journal/Retainer file writes all serialized under `_fileLock`; BackupService's file replace takes the same lock.
- **key findings (from this session):** (a) `IPlayerState.HomeWorld.ValueNullable?.Name.ExtractText()` is the canonical home-world source, resolves immediately post-login. (b) `PriceDslContext` was updated with `MinPriceHw`/`AvgPriceHw` optional parameters (defaulted to 0 so existing formulas keep working). (c) `Configuration.Sync` is JsonIgnored so it doesn't hit disk. (d) `WatchlistPoller.AttachPlugin` late-bind pattern breaks the Plugin↔Poller ctor cycle cleanly.
- **next_step:** User still wants more feature edits before Phase 7. Await user direction. When Phase 7 does start: verify home-world sourcing works (RetainersTab should show `*` on any item where you don't have a home-world listing), test formula parsing with `mbminhw` in a real formula, exercise Backup restore round-trip after making trades, watch `/xllog` for new Universalis diagnostic lines.
- **blockers:** RetainerSellHistory real parser still awaits the probe log dump — Session M item #2 stays in queue until that lands.
- **open_questions:**
  - Home-world logic falls back to DC-wide silently. Consider: do we want the fallback at all for the buy-signal trigger? (Currently: yes, so alerts still fire for items nobody's listing on your realm.)
  - KupoTradeMaster name still placeholder.
  - GitHub repo `RepoUrl` still aspirational.

- **what_not_to_rethink:**
  - **Toolchain confirmed working**: Dalamud.NET.Sdk/15.0.0 + .NET 10.0.101 + LangVersion 14 auto-set by SDK; `Dalamud.Bindings.ImGui` (NOT `ImGuiNET`); DalamudPackager auto-generates the manifest — do not hand-write one.
  - Output packaging: SDK produces `KupoTradeMaster.dll` + `KupoTradeMaster.json` + `KupoTradeMaster/latest.zip`. Sideload from either.
  - Data spine: Universalis REST (25 r/s hard cap; we use 40 IDs/batch, 2 concurrent). WebSocket deferred.
  - v0 pillars status: All four now **done** — Watchlist+FlipScore, Journal, Price DSL, Tooltip (via overlay). Crafting is a bonus fifth pillar.
  - NOT building: auto-undercutter, mail-alt distribution, bulk MB scans, retainer post/cancel, tooltip node-injection. Zero automated actions in v1 (repo eligibility).

**last session:** 2026-09-06 · code-review pass + home-world pricing refactor + rename (Kupomart → KupoTradeMaster: src/KupoTradeMaster/, KupoTradeMaster.sln, namespace KupoTradeMaster.*, `/ktm` + `/kupo` commands, RepoUrl foglerk/kupotrademaster). Release 0.1.0 built clean.
